
# Bloquear acesso direto a pastas sensíveis
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /nata-do-campo/
    
    # Redirecionar tudo para a pasta public
    RewriteCond %{REQUEST_URI} !^/nata-do-campo/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Bloquear listagem de diretórios
Options -Indexes
